# 도서 광고 제작 프로젝트

도서 표지·삽화를 활용한 홍보 숏폼과 키네틱 타이포 영상에는 먼저 `.agents/skills/book-kinetic-ad/SKILL.md`를 읽고 적용한다. HyperFrames의 구조·렌더 규칙도 함께 따른다. 영상과 무관한 작업에는 강제하지 않는다.

- 사용자가 지정한 독자·말투·규격·수정 범위가 기본값보다 우선한다.
- Paperlogy·Lottie의 표현 원리를 재사용하되 도서명·혜택·장면 순서·BGM을 다른 책에 자동 이식하지 않는다.
- 새로운 작업은 `projects/<세 자리 순번>-<작업명>/`, 영상은 그 아래 `videos/<영상명>/`에 둔다. 로컬 폴더를 확인해 다음 순번을 정한다. 기존 작업의 수정본은 같은 작업 폴더에 둔다.
- 제공 원본은 `source-assets/`, 렌더용 파일은 각 영상의 `assets/`에 보존한다. 이전 결과물을 덮어쓰지 않는다.
- 공통 스킬·시작 코드는 `.agents/`에 유지한다. 제작물은 GitHub 업로드 범위가 아니다.
- 원본 표지·음원·완성 MP4·생성된 HTML/JSON·캐시·인증 정보는 커밋하지 않는다. `.gitignore`를 해제하거나 강제로 추가하지 않는다.
- 필요한 렌더 검증과 실제 프레임 검수를 마치고 최종 파일과 편집 소스를 안내한다.

새 클론에는 기존 책의 원본·완성 영상이 없다. 선택적인 로컬 참조 자료가 없으면 스킬의 글로 된 원칙과 제공된 새 도서 자료로 진행한다. 기본 시작 스크립트는 이 레포에 포함된 파일만으로 실행된다.
