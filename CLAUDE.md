# 도서 키네틱 광고 제작

공통 프로젝트 규칙은 [AGENTS.md](AGENTS.md)를 따른다. 도서 광고를 만들거나 수정할 때는 [book-kinetic-ad](.agents/skills/book-kinetic-ad/SKILL.md)를 먼저 읽는다.

## 제작 흐름

1. 사용자가 지정한 독자와 목적을 확인하고, 제공된 표지·소개·목차에서 카피의 근거를 찾는다. 이미 제공된 내용은 다시 묻지 않는다.
2. HyperFrames 스킬로 제작 방식을 정한다. 설치되어 있지 않으면 `npx hyperframes skills update general-video`로 준비한다.
3. 새 작업은 공통 시작 스크립트로 만들고 BRIEF와 `scripts/composition.mjs`에 그 책의 연출을 작성한다. 전체 장면을 이전 책에서 복제하지 않는다.
4. 대표 화면과 핵심 전환을 확인한 뒤 전체로 확장한다. 글자 정렬·비례·읽는 정지·장면 연결을 실제 프레임으로 검수한다.
5. 대상과 분위기에 맞는 상업 이용 가능한 BGM을 찾고 적용 조건과 사용 구간을 MUSIC.md에 기록한다. 직접 듣지 못한 경우 청취 검수를 했다고 말하지 않는다.
6. `npm run build`, `npm run check`, `npm run render`를 실행하고 MP4 영상·오디오·길이·디코드를 확인한다.

## 편집 원칙

- 한정된 수정이면 요청한 카피·음원·장면만 고친다.
- 성인 교사를 대상으로 할 때는 전문성을 존중하는 친근한 말투를 쓴다. 대상이 교사라는 이유로 아동용 말투나 음악을 자동 선택하지 않는다.
- ‘8가지’ 같은 수량을 강조하면 화면의 제시 방식과 맞춘다. 일부만 보여주려면 예시라는 점을 분명히 한다.
- 원본 표지·상표·인물은 정확히 보존한다. 자산이 없다고 비슷한 로고나 얼굴을 임의로 재현하지 않는다.
- 의미 없이 글자를 늘리거나 흔드는 효과를 반복하지 않는다. 읽는 순간의 조판이 먼저다.

## 저장소 범위

이 레포는 제작 도구와 지침만 보관한다. `projects/`의 원본·음원·완성 영상은 로컬에서 관리한다. 설치한 npm 패키지, 빌드 산출물, 인증 정보도 업로드하지 않는다.
기본 시작본에는 폰트와 npm 설정이 있고 `npm ci` 후 Lottie·GSAP가 로컬 렌더 자산으로 준비된다. 제3자 폰트와 라이브러리의 라이선스를 유지한다.
